server:
  port: 8080
spring:
  application:
    name: inbound-data-processor
inbound: src/test/resources/cold/inbound
archive: src/test/resources/cold/archive
stop: src/test/resources/stop.1
  dir:
process:
  files: all
extension:
  file:
    trig: .trig
    lock: .lock
    error: .err
retry: 10
